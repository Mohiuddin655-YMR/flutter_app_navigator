name: flutter_app_navigator
description: This is a navigator, route and screen movement library.
version: 1.0.0
homepage: https://github.com/P-YMR/flutter_app_navigator.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter: